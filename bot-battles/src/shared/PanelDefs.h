#ifndef __PANEL_DEFS_H__
#define __PANEL_DEFS_H__

#include "PanelTypes.h"

namespace sand {

const U16 MAX_PANELS = static_cast<U16>(PanelType::COUNT);
const U16 INVALID_PANEL = MAX_PANELS;
}

#endif
